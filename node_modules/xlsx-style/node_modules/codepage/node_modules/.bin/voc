#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_voc@1.2.0@voc/voc.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_voc@1.2.0@voc/voc.njs" "$@"
  ret=$?
fi
exit $ret
